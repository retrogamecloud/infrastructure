apiVersion: v1
kind: Secret
metadata:
  name: argocd-secret
  namespace: argocd
  labels:
    app.kubernetes.io/name: argocd-secret
    app.kubernetes.io/part-of: argocd
type: Opaque
stringData:
  # GitHub OAuth Client Secret
  # IMPORTANTE: Reemplazar con el valor real del GitHub OAuth App
  dex.github.clientSecret: "GITHUB_OAUTH_CLIENT_SECRET_AQUI"
  
  # Server signature (se genera automáticamente si no existe)
  # server.secretkey: ""
