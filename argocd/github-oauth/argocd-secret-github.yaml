apiVersion: v1
kind: Secret
metadata:
  name: argocd-secret
  namespace: argocd
  labels:
    app.kubernetes.io/name: argocd-secret
    app.kubernetes.io/part-of: argocd
type: Opaque
stringData:
  # GitHub OAuth Client Secret
  # REUTILIZA el mismo secret que oauth2-proxy en monitoring namespace
  # Copiado desde: kubectl get secret oauth2-proxy -n monitoring -o jsonpath='{.data.client-secret}' | base64 -d
  dex.github.clientSecret: "0e14645efdb0317bd3c83c9ace9a76f198b53052"
  
  # Server signature (se genera automáticamente si no existe)
  # server.secretkey: ""
