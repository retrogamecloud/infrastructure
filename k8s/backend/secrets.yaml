# This is a template file. Actual secrets should be managed via:
# 1. Kubernetes secrets created manually
# 2. External Secrets Operator
# 3. Sealed Secrets
# 4. AWS Secrets Manager with IRSA
#
# DO NOT commit actual secrets to Git!

apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
  namespace: retrogameargo
type: Opaque
stringData:
  db-host: "REPLACE_WITH_RDS_ENDPOINT"
  db-name: "retrogame_db"
  db-user: "REPLACE_WITH_DB_USER"
  db-password: "REPLACE_WITH_DB_PASSWORD"
  jwt-secret: "REPLACE_WITH_JWT_SECRET"
