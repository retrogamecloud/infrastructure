# Configuración del backend S3 desde bootstrap
# Usar con: terraform init -backend-config=backend.conf

# Obtener estos valores ejecutando:
# cd ../bootstrap && terraform output

bucket         = "retrogamecloud-terraform-state-450545962171"  # terraform output terraform_state_bucket
region         = "eu-west-1"
dynamodb_table = "terraform-lock"
profile        = "retrogamecloud-terraform"
#profile        = "default"
